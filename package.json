{
  "name": "fly-circuit-arena",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Three connectome-derived fly circuits in one shared live arena",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "wrangler dev --ip 127.0.0.1 --port 8787",
    "test": "node --test tests/*.test.js",
    "experiment": "node scripts/experiment.js",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "0.186.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "miniflare": "^5.20260911.0-alpha",
    "vite": "8.3.0",
    "wrangler": "4.131.1"
  }
}
